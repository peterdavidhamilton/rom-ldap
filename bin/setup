#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

# increase file limit
ulimit -n 1024

# prepare specs
bundle install

# prepare examples
cd example
bundle install

# prepare benchmarks
cd ../benchmark
bundle install



# LDIF file merge
"include: file:///path/to/rom-ldap/examples/ldif/schema.ldif"

