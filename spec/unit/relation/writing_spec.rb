require 'spec_helper'

describe ROM::LDAP::Relation, 'writing module' do
  include RelationSetup

  # let(:user_name) { Faker::Internet.unique.user_name }

  it '#insert' do
  end

  it '#update' do
  end

  it '#delete' do
    # accounts.where(uid: user_name).delete
  end
end
