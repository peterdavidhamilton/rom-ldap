RSpec.describe ROM::LDAP::Connection do

  include_context 'directory'

  xit '#bind' do
    expect(directory).to
  end

  xit '#add' do
    expect(directory).to
  end

  xit '#delete' do
    expect(directory).to
  end

  xit '#modify' do
    expect(directory).to
  end

  xit '#password_modify' do
    expect(directory).to
  end

  xit '#rename' do
    expect(directory).to
  end

  xit '#search' do
    expect(directory).to
  end


end
